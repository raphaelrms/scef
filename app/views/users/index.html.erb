<ol class="breadcrumb">
  <li><a href="<%= root_path%>">Principal</a></li>
  <li class="active">Usuários</li>
</ol>
<%= painel_titulo("Usuários").html_safe %>
<div class="panel-body">
<div class="span8">
<table class="table table-condensed">
  <thead>
    <tr>
      <th>Usuário</th>
      <th>Nome</th>
      <th>E-mail</th>
      <th>Data de Cadastro</th>
      <th>Grupo</th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  <% @users.each do |user| %>
    <tr>
      <td><%= link_to user.name, user %></td>
      <td><%= user.name%></td>
      <td><%= user.email %></td>
      <td><%= user.created_at.to_date %></td>
      <td><%= user.roles.first.name.titleize unless user.roles.first.nil? %></td>
      <td>
        <a data-toggle="modal" href="#role-options-<%= user.id %>"  type="button">Change role</a>
        <%= render user %>
      </td>
      <td><%= link_to("Delete user", user_path(user), :style => "margin-top:-7px;", :data => { :confirm => "Are you sure?" }, :method => :delete, :class => 'btn btn-mini') unless user == current_user %></td>
    </tr>
  <% end %>
  </tbody>
</table>
</div>
</div>
</div>