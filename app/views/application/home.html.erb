# encoding: utf-8
<ol class="breadcrumb">
  <li><a href="<%= root_path%>">Principal</a></li>
</ol>
<%= painel_titulo("Quadro de avisos").html_safe %>
<div class="panel-body">
<% if @noticias %>
    <div class="noticia">
      <ul class="list-unstyled">
        <% @noticias.each do |noticia| %>
            <p>
            <li>
            <div class="col-md-6">
              <div class="box box-element">
                <div class="view">
                  <h2 contenteditable="false"><%=noticia.titulo%></h2>
                  <small contenteditable="false">Postado em: <%= l noticia.created_at.to_date %></small>
                  <p contenteditable="false"><%= noticia.corpo_resumido.html_safe %></p>
                  <p><a class="btn" href="#" contenteditable="false"><%= link_to "[Saiba mais]", noticia %></a></p>
                </div>
            </div>
              </div>
            </li>
            </p>
        <% end %>
      </ul>

    </div>
    <%= will_paginate @noticias %>
<% else %>
    <p>Nenhuma notÃ­cia postada atÃ© o momento.</p>
<% end %>
  <%= link_to "Adicionar aviso", new_noticia_path, :class => "btn btn-success"%>
                        </div>

                        </div>